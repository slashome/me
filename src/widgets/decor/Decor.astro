---
import './decor.css';
import Character from './Character.astro';

export type DecorAnim = 'draw' | 'idle' | 'vibrate';

interface Props {
  anim?: DecorAnim;
}

const { anim = 'draw' } = Astro.props;
---

<div class="decor-scene" data-anim={anim} aria-hidden="true">
  <Character />
</div>
