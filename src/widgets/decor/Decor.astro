---
import './decor.css';
import Alley from './scenes/Alley.astro';

export type DecorScene = 'alley';

interface Props {
  scene?: DecorScene;
}

const { scene = 'alley' } = Astro.props;
---

<div class="decor-scene" data-scene={scene} aria-hidden="true">
  {scene === 'alley' && <Alley />}
</div>
